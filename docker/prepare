#!/usr/bin/env bash
set -euo pipefail

script_dir="$(cd -- "$(dirname -- "${BASH_SOURCE[0]}")" && pwd)"
repo_root="$(cd -- "$script_dir/.." && pwd)"

PYTHONPATH="$repo_root" exec python3 "$script_dir/prepare_task.py" "$@"
