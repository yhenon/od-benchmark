[project]
name = "od-benchmark"
version = "0.1.0"
description = "An agentic benchmark for energy-constrained computer vision optimization"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "colorama>=0.4.6",
    "onnx>=1.22.0",
    "protobuf>=4.25.1",
    "pyserial==3.4",
    "tqdm==4.50.2",
]
